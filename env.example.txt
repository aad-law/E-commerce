# Environment Variables for Nagaare Gold Jewellers

# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/nagaare?retryWrites=true&w=majority

# NextAuth Configuration
NEXTAUTH_SECRET=your-super-secret-key-change-this-in-production
NEXTAUTH_URL=http://localhost:3000

# Admin Email (for initial admin account)
ADMIN_EMAIL=admin@nagaare.com
ADMIN_PASSWORD=admin123

# Razorpay (Payment Gateway)
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Email Configuration (for contact form, order notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
